---
description: >-
  Required Discovery carpool dismissal context. Always apply in this repo before
  touching code. Agent chat is English; app UI/store strings stay Spanish-first.
alwaysApply: true
---

# Required context (carpool)

Before any change in this repository, read `.opencode/skills/carpool/SKILL.md` with the
file-read tool: it has the full product context — core idea
("we adapt the software to the school"), real constraints (continuous carpool line,
staggered dismissal by grade, no on-site screens, teachers with 1 tap, no
"on the way" announcements, no walk-ins), surfaces (`/familia`, `/kiosco`, `/personal`,
`/admin`, `/pantalla`), architecture (Snapshot as contract, dual memory/Supabase store,
2s polling, state machine, late pickups with no states, friend families), file map,
conventions (zero comments, Spanish-first UI, gold/forest/danger tokens, do not revive
"Bitácora" or "En el kiosco"), and environment quirks.

Summarize the context in at most three **English** lines and confirm the task before editing.

Language: agent ↔ human (chat, reviews, status, commits, PR bodies) = **English only**.
App / user-facing code = **Spanish-first** (UI copy, staff surfaces, store errors).
`/familia` may keep ES/EN; staff = Spanish. Do not reply in Spanish.
